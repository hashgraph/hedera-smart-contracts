services:
  slither:
    build: slither-analysis # WITH ecrecover DETECTOR preinstalled!
    volumes:
      - ./test_contracts:/examples
    extra_hosts:
      - "host.docker.internal:host-gateway"
    entrypoint: ["sleep", "infinity"]
